# Frontend API base URL
NEXT_PUBLIC_API_URL=http://localhost:8000/api/v1

# Optional: explicit OAuth redirect URI for mailbox callback.
# OAuth client IDs/secrets are now server-side in server/.env.
NEXT_PUBLIC_MAIL_OAUTH_REDIRECT_URI=http://localhost:3000/mailbox
